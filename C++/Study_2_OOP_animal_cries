#include <iostream>
#include <string>

using namespace std;

class Animal
{
public:
    Animal() {}
    virtual void makesound() {}
    virtual ~Animal() {}
};

class Dog : public Animal
{
public:
    Dog(string word) : m_word(word) {}
    void makesound() override { cout << "Dog roars: " << m_word << endl; }
private:
    string m_word;
};

class Cat : public Animal
{
public:
    Cat(string word) : m_word(word) {}
    void makesound() override { cout << "Cat meows: " << m_word << endl; }
private:
    string m_word;
};

class Cow : public Animal
{
public:
    Cow(string word) : m_word(word) {}
    void makesound() override { cout << "Cow mooing: " << m_word << endl; }
private:
    string m_word;
};

int main()
{
    Animal* animals[3];

    animals[0] = new Dog("woofwoof!");
    animals[1] = new Cat("meowww!");
    animals[2] = new Cow("mooooo!");

    for (int i = 0; i < 3; ++i) {
        animals[i]->makesound();
    }

    for (int i = 0; i < 3; ++i) {
        delete animals[i];
    }

    return 0;
}
